# 🚀 리눅스 ffmpeg 설치/확인/삭제

```shell
sudo apt update
sudo apt install ffmpeg -y
```

```shell
ffmpeg -version
```

```shell
which ffmpeg
```

```shell
sudo apt purge ffmpeg -y
```

```shell
sudo apt autoremove -y
```

```shell
which ffmpeg
ffmpeg -version
```
