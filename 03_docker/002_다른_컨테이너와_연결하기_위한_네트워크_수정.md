# 🚀 별도의 컨테이너와 연결하기 위한 Networks 수정 작업

## 현재의 상황

- 두 프로젝트 A(메인 API + DB + Qdrant), B 프로젝트(데이터 인서트/수정 등)가,
  서로 다른 네트워크를 사용하고 있다.

즉, B 프로젝트에서 A 프로젝트의 컨테이너(Postgres, Qdrant, Redis, Celery) 등에 접근해야 하는
상황이다.

## 프로젝트가 서로의 네트워크에 참여해야 한다

```compose.yml
# A 프로젝트
networks:
  shopper-local-network:
    driver: bridge
```

```compose.yml
# B 프로젝트
networks:
  shopper_dev_net:
    external: true
    name: shopper-dev_default
```

## 해결방법 step by step

아래의 명령어로 사용중인 network 확인

```shell
docker network ls
```

```shell
services:
  data-app:
    ...
    networks:
      - shopper_shared_net

networks:
  shopper_shared_net:
    external: true
    name: shopper-dev_shopper-local-network # 확인 후 설정한 네트워크
```

위와 같이 수정 후 docker compose down / up 실행

## B 프로젝트 컨테이너 내부에서 테스트

```shell
ping shopper-db
ping shopper-qdrant
ping shopper-redis
```
